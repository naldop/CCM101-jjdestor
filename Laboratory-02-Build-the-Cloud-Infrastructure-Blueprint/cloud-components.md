# Cloud Infrastructure Components

## Compute Resources
- Purpose: Executes processing tasks (the CPU/cores you found).
- Why it matters: Determines how much workload the server can handle simultaneously.
- In my environment: The KillerCoda VM has (X cores), used to run my terminal sessions and commands.

## Storage Resources
- Purpose: Persists data (disk, filesystems).
- Why it matters: Cloud apps need reliable storage for OS files, logs, and user data.
- In my environment: Disk capacity was (X), mounted on (filesystem paths found).

## Networking Resources
- Purpose: Connects the server to other systems/the internet (hostname, IP).
- Why it matters: Without networking, cloud services can't be reached by users or other servers.
- In my environment: Hostname (X), IP (X), used to identify and reach this instance.

## Operating System
- Purpose: Manages hardware resources and runs software (Ubuntu, kernel).
- Why it matters: The OS is the foundation every cloud service and app runs on top of.
- In my environment: Running (OS name/version), kernel (X).